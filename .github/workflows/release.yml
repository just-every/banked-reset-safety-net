name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate:
    name: Validate release metadata
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      package-version: ${{ steps.version.outputs.package-version }}

    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Validate version tag
        id: version
        run: node scripts/release/validate-version-tag.mjs

  macos:
    name: macOS universal (signed and notarized)
    needs: validate
    runs-on: macos-15
    timeout-minutes: 45
    env:
      MAC_CSC_LINK: ${{ secrets.MAC_CSC_LINK }}
      CSC_KEY_PASSWORD: ${{ secrets.MAC_CSC_KEY_PASSWORD }}
      APPLE_API_KEY_P8: ${{ secrets.APPLE_API_KEY_P8 }}
      APPLE_API_KEY_ID: ${{ secrets.APPLE_API_KEY_ID }}
      APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER }}

    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Require Apple signing and notarization credentials
        run: scripts/release/prepare-macos-credentials.sh

      - name: Install pnpm
        uses: pnpm/action-setup@v6

      - name: Install Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build signed and notarized universal app
        run: pnpm dist:mac

      - name: Verify signature, notarization, and packaged apps
        run: scripts/release/verify-macos-artifacts.sh

      - name: Upload macOS release assets
        uses: actions/upload-artifact@v7
        with:
          name: release-macos-universal
          if-no-files-found: error
          retention-days: 14
          path: |
            release/Banked-Reset-Safety-Net-mac-universal.dmg
            release/Banked-Reset-Safety-Net-mac-universal.zip
            release/Banked-Reset-Safety-Net-mac-universal.zip.blockmap
            release/latest-mac.yml
            release/SHA256SUMS-macos.txt

  windows:
    name: Windows ${{ matrix.arch }}
    needs: validate
    runs-on: ${{ matrix.arch == 'x64' && 'windows-2025' || 'windows-11-arm' }}
    timeout-minutes: 35
    strategy:
      fail-fast: false
      matrix:
        arch:
          - x64
          - arm64

    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Install pnpm
        uses: pnpm/action-setup@v6

      - name: Install Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
          architecture: ${{ matrix.arch }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Windows ${{ matrix.arch }} installer
        run: pnpm dist:win:${{ matrix.arch }}

      - name: Verify Windows installer output
        shell: pwsh
        run: scripts/release/verify-windows-artifacts.ps1 -Architecture "${{ matrix.arch }}"

      - name: Upload Windows ${{ matrix.arch }} release asset
        uses: actions/upload-artifact@v7
        with:
          name: release-windows-${{ matrix.arch }}
          if-no-files-found: error
          retention-days: 14
          path: |
            release/Banked-Reset-Safety-Net-win-${{ matrix.arch }}.exe
            release/Banked-Reset-Safety-Net-win-${{ matrix.arch }}.exe.blockmap
            release/latest*.yml
            release/SHA256SUMS-windows-${{ matrix.arch }}.txt

  bundle:
    name: Verify complete release bundle
    needs:
      - validate
      - macos
      - windows
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Download verified release assets
        uses: actions/download-artifact@v8
        with:
          pattern: release-*
          path: dist
          merge-multiple: true

      - name: Verify complete asset set and create combined checksums
        run: node scripts/release/prepare-release-assets.mjs dist "${{ needs.validate.outputs.package-version }}"

      - name: Upload verified release bundle
        uses: actions/upload-artifact@v7
        with:
          name: release-bundle
          if-no-files-found: error
          retention-days: 14
          path: |
            dist/Banked-Reset-Safety-Net-mac-universal.dmg
            dist/Banked-Reset-Safety-Net-mac-universal.zip
            dist/Banked-Reset-Safety-Net-mac-universal.zip.blockmap
            dist/Banked-Reset-Safety-Net-win-x64.exe
            dist/Banked-Reset-Safety-Net-win-x64.exe.blockmap
            dist/Banked-Reset-Safety-Net-win-arm64.exe
            dist/Banked-Reset-Safety-Net-win-arm64.exe.blockmap
            dist/latest-mac.yml
            dist/latest.yml
            dist/latest-arm64.yml
            dist/SHA256SUMS.txt

  publish:
    name: Publish GitHub release
    if: startsWith(github.ref, 'refs/tags/v')
    needs:
      - validate
      - bundle
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    env:
      GH_TOKEN: ${{ github.token }}

    steps:
      - name: Download verified release bundle
        uses: actions/download-artifact@v8
        with:
          name: release-bundle
          path: dist

      - name: Publish release
        run: |
          gh release create "$GITHUB_REF_NAME" \
            dist/Banked-Reset-Safety-Net-mac-universal.dmg \
            dist/Banked-Reset-Safety-Net-mac-universal.zip \
            dist/Banked-Reset-Safety-Net-mac-universal.zip.blockmap \
            dist/Banked-Reset-Safety-Net-win-x64.exe \
            dist/Banked-Reset-Safety-Net-win-x64.exe.blockmap \
            dist/Banked-Reset-Safety-Net-win-arm64.exe \
            dist/Banked-Reset-Safety-Net-win-arm64.exe.blockmap \
            dist/latest-mac.yml \
            dist/latest.yml \
            dist/latest-arm64.yml \
            dist/SHA256SUMS.txt \
            --repo "$GITHUB_REPOSITORY" \
            --verify-tag \
            --generate-notes \
            --title "Banked Reset Safety Net $GITHUB_REF_NAME"
